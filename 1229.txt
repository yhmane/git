12/29hwang9yh/kaoni
* ezFlow4Java setting
0. jdk 설치
1. git 설치 
2.DB설치 -> mysqlm, mysqlWorkbench 설치
3.전자정부 프레임워크 3.5.1 개발환경 설치 
   git으로 ezFlow4Java import
프로젝트를 import 했으나 수많은 에러가 발생
	
	1) building workspace 문제 => 힙 메모리가 부족하여 생김
	   => eclipse.ini 파일 설정 변경
		-XX:MaxPermSize=256m     //추가
		-Xms512m                 // 40을 512로
		-Xmx1024m                // 200몇을 1024로 바꿔줌
	힙 메모리 부족 문제 해결


	2) maven을 설치 후 update project 실패..
		C:\Users\로그인계정\.m2\repository\org\apache\maven\plugins\maven-resources-plugin 밑에 2.6 폴더를 삭제 후 재구동
	maven 문제 해결


	3) Validation 문제도 생김
	-1 DTD Validator setting을 해줌 -> Add Exclude Group -> Add Rule -> Folder:/src/main/webapp/files
	-2 JSP Syntax Validator setting -> Add Exclude Group -> Add Rule 
		-> Folder: src/main/webapp/js/ezEditor/dext5Editor
		   Folder: src/main/webapp/js/ezEditor/namoEditor
	Validation 문제 해결


	4) obsolete 문제 -> 버전이 문제...

* Linux설치 (CentOS7 version) 후, ezPKP 패키지 설치

	Virtual Box 설치
	CentOS7 설치
	게스트 확장 설치까지 진행
	추후, ezPKP 설치 매뉴얼 보고 설치 예정
?	vi /etc/sysconfig/network-scripts/ifcfg-네트워크인터페이스이름enp0s3

ln
ln은 Link 의 약어로서 리눅스 파일시스템에서 링크파일을 만드는 명령어이다. 리눅스에서는 심볼릭링크와 하드링크의 두가지 링크 파일이 존재한다.
심볼릭링크 (Symbolic Link)
단순히 원본파일을 가리키도록 링크만 시켜둔 것으로 MS의 윈도우시스템에서 흔히 사용하는 '바로가기' 같은 것이며, 원본파일을 가리키고만 있으므로 원본파일의 크기와는 무관한다.
하드링크 (Hard Link)
원본파일과 다른 이름으로 존재하는 동일한 파일이며 원본파일과 동일한 내용의 다른 파일이라고 할 수 있다.
-s : 심볼릭링크(소프트링크)를 생성한다. 만약 이 옵션이 없으면 하드링크가 생성된다.
-f, --force                 remove existing destination files
  -n, --no-dereference        treat destination that is a symlink to a
                                directory as if it were a normal file


* 사다리게임 회의(김보혜 사원과)
	=> 페이지 구성, 기능 추가, 레이아웃에 대하여 회의하였고, 회의 내용을 토대로 각자 design prototype 작업 
	=> 둘 다 레이아웃 완성. => 차주 합치고 수정할 예정

	